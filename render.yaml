services:
  - type: web
    name: portfolio-chatbot
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 3000
